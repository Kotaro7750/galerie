# Documentation

- 設計に関する仕様・判断・構成は、リポジトリルートの `docs/` ディレクトリ配下に Markdown ファイル（`.md`）として記録してください。
- 設計を追加または変更した場合は、対応するドキュメントも作成または更新してください。
- `docs/` ディレクトリが存在しない場合は作成してください。
- `docs/` 配下のファイル名で単語を区切る場合は、アンダーバー（`_`）を使用してください。

# Knowledge
上記のように、本リポジトリにおいては`docs/`ディレクトリ配下に設計ドキュメント階層を配置しています。
そのため、いかなる調査・判断を行う場合においても、一般論ではなく関連する既存ドキュメントを確認するようにしてください。

その際、毎回全ドキュメントを読むのではなく、`docs/overview.md`を読むことでドキュメントの概要を把握し、質問に関連するドキュメントを特定したうえで、その内容を確認してください。
